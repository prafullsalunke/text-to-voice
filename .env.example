MODEL_ID=openbmb/VoxCPM2
PORT=8000
TEXT_MAX_LENGTH=500
# HF_TOKEN=your_token_here   # Required if model is gated on HuggingFace
# API_SECRET=<RSA private key PEM>  # Signs JWTs (keep secret, used offline to issue tokens)
# API_TOKEN=<RSA public key PEM>    # If set, all endpoints verify Bearer JWT signatures against this key
